apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: carmen-ingress
  namespace: proyectocarmen
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: carmenasir.es
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: carmen-service
                port:
                  number: 80
    - host: www.carmenasir.es
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: carmen-service
                port:
                  number: 80
