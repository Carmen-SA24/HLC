FROM ubuntu
ARG USUARIO
# ARG PASSWORD
# RUN apt update && apt install -yq --no-install-recommends \ apache2 \ nano \ curl \ net-tools iputils-ping
# **** Instalaciones *****
RUN apt update && apt install -yq --no-install-recommends \ 
nano \
git  curl  wget \
iputils-ping net-tools \
sudo

# copiar los srcipt's del entrypoint (sc pcpal --> sc' perosonalizados)
RUN mkdir -p /root/admin
COPY ./dockerfiles/base/admin/start.sh /root/admin
RUN chmod +x /root/admin/start.sh


# el primer comando que use el contenedor para manenerse vivo
# CMD ["tail", "-f", "/dev/null"]

ENTRYPOINT [ "/root/admin/start.sh" ]