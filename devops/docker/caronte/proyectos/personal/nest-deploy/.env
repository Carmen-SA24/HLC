# Contexto de build (ruta al directorio Caronte en el VPS)
CONTEXTO=/home/rosa/devops/docker/caronte

# Variables del proyecto
PROYECTO=nestapi
TIPO=personal
INICIALES=crsa
FIRMA=carmensalirrosas

# Variables de usuario y seguridad (heredadas por ubbase)
USUARIO=rosa
PASSWORD=1234
PORT_SSH=2228
PORT_SSH_PG=2223

# Variables de Base de Datos (usadas por PostgreSQL y NestJS)
DB_NAME=nestasir_db
DB_USERNAME=admin
DB_PASSWORD=password
DB_HOST=postgres
DB_PORT=5432

# Puertos
PORT_NODE=3015
PORT_POSTGRES=5433

# Puerto de la aplicacion NestJS
PORT=3001

# Contenedor
CONTENEDOR=ct${INICIALES}${PROYECTO}
